apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: self-intro

resources:
  - ../../../base/frontend
  - ingress.yaml
  - hpa.yaml

patches:
  - path: patch-scheduling.yaml

images:
  - name: self-intro-frontend
    newName: yny.ocir.io/axrywc89b6lf/self-intro/frontend
    newTag: "0127ec4"
